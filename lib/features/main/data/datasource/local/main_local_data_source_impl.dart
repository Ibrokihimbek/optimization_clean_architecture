part of "main_local_data_source.dart";

class MainLocalDataSourceImpl implements MainLocalDataSource {
  const MainLocalDataSourceImpl({
    required this.localSource,
  });

  final LocalSource localSource;
}
